# External provider catalog mount

Place the accepted provider catalog in this directory only for a local run.
The directory must contain a real `manifest.json` and the catalog files
referenced by that manifest. This repository intentionally does not include a
fabricated catalog.
